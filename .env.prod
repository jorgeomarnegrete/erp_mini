# Cambia estas contraseñas por unas seguras en el servidor real
DB_USER=admin
DB_PASSWORD=IntiMayu_Prod_Secure
DB_NAME=appdb

# Base de datos en Docker
DATABASE_URL=postgresql://${DB_USER}:${DB_PASSWORD}@db/${DB_NAME}

# Frontend (Producción en el Servidor)
VITE_API_URL=http://179.43.123.119
